# Email Spam Classifier

This project is an **Email Spam Classifier** that predicts whether an email is spam or not.  
It uses **Natural Language Processing (NLP)** and **Naive Bayes algorithms** to classify emails.

## Features

- Implements **Multinomial, Bernoulli, and Gaussian Naive Bayes** models.
- Uses **CountVectorizer** and **TF‑IDF** for text feature extraction.
- Preprocessing steps include:
  - Lowercasing
  - Removing punctuation
  - Tokenization
- Provides accuracy evaluation and confusion matrix for performance.


